/** Dedicated admin flow for the storefront homepage hero strip (not social campaigns). */
export const ADMIN_MARKETING_HOMEPAGE_BANNER = "/admin/marketing/homepage-banner";

/** Settings → Marketing channels (Meta / WhatsApp configuration). */
export const ADMIN_SETTINGS_MARKETING_CHANNELS = "/admin/settings/marketing-channels";

/** Admin route that starts Facebook OAuth (middleware-protected). */
export const ADMIN_SETTINGS_MARKETING_CHANNELS_FB_START =
  "/admin/settings/marketing-channels/facebook/start";
